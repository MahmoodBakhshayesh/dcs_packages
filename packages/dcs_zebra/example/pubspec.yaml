name: dcs_zebra_example
description: Example for dcs_zebra TCP connect / status / print.
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dcs_zebra:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
